import React, { Component } from 'react';

class Form extends Component {
    state = {  }
    render() {
        return (
            <div>Form component</div>
        );
    }
}

export default Form;